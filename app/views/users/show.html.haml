.row
	%h1.col-md-12=@user.username
	/ .user-page-delete-link= link_to 'Delete this account', method: :destroy
	.user-logout-link= link_to 'Logout', logout_path, method: :delete
	- if logged_in? && @user == current_user
		.user-delete-link= link_to 'Delete my account', user_path(@user), method: :delete, data: { confirm: "Are you sure?" }
	.user-link-to-users= link_to 'All users', users_path
.row
	%h2.col-md-12 Blogs:
- if @user.blogs.count > 0
	- @user.blogs.each do |blog|
		.row.user-page-row
			.col-md-4
			%h3.col-md-4.blog-link= link_to blog.title, user_blog_path(@user, blog)
			- if logged_in? && @user == current_user
				%h4.col-md-2= link_to 'Delete this blog', user_blog_path(@user, blog), method: :delete, data: { confirm: "Are you sure? " }
				%h4.col-md-2= link_to "Change this blog's title", edit_user_blog_path(@user, blog)
- else
	%h3 No blogs? Make one!
- if logged_in? && @user == current_user
	.user-create-blog-link= link_to 'Create a blog', new_user_blog_path(@user)